# Bouclier Bleu Core Configuration

[modules]
# Modules explicitly enabled at startup.
# Any module not listed here will be loaded but remain completely dormant 
# (returning 0 in the kernel) until manually enabled via the CLI.
exec_block = true
shield = true
mount_secure = true
